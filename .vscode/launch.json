{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch your_app (CMake/MSVC)",
      "type": "cppvsdbg",      
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceRoot}",           
      "environment": [],
      "console": "externalTerminal",     
      // "preLaunchTask": "CMake: build"
    }
  ],
  "compounds": [
    {
      "name": "Build and Launch",
      "configurations": ["Launch your_app (CMake/MSVC)"],
      "preLaunchTask": "Cmake: build"     
    }
  ]
}